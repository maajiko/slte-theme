# SLTE 一键编译工作流（由 AppForge 平台写入 fork 仓库）
# appforge-workflow-version: 2026.09.1
#
# 配置注入方式与 app/gradle.properties.example 完全对齐：
#   网页表单 → workflow_dispatch inputs → SLTE_* 环境变量 → app 构建脚本读取。
# 所有配置仅在触发编译那一刻经 API 传入，不写入 Git 仓库。
# 签名凭据只经仓库 Secrets（SLTE_RELEASE_*），不出现于 inputs 或本文件。

name: SLTE Build

on:
  workflow_dispatch:
    inputs:
      appName:
        description: 应用名称（桌面显示名）
        required: true
        default: SLTE
      applicationId:
        description: 包名
        required: true
        default: com.slte.app
      versionName:
        description: 版本号
        required: true
        default: 1.0.0
      versionCode:
        description: versionCode（留空自动生成）
        required: false
        default: ''
      apiBaseUrl:
        description: 面板 API 地址
        required: true
      apiType:
        description: 面板版本
        type: choice
        options:
          - xiaov2b
          - xboard
        default: xiaov2b
      remoteConfigUrls:
        description: 远程动态地址（逗号分隔，最多 3 个）
        required: false
        default: ''
      allowedDomains:
        description: 追加白名单域名（逗号分隔）
        required: false
        default: ''
      notificationTitle:
        description: 通知栏标题（留空跟随应用名）
        required: false
        default: ''
      notificationTraffic:
        description: 通知栏流量显示
        type: choice
        options:
          - 'true'
          - 'false'
        default: 'true'
      crispWebsiteId:
        description: Crisp 客服 ID（留空关闭）
        required: false
        default: ''
      crispEnabled:
        description: 是否启用 Crisp 客服
        type: choice
        options:
          - 'true'
          - 'false'
        default: 'false'
      buildType:
        description: 构建类型（release 需仓库 Secrets 签名四件套）
        type: choice
        options:
          - debug
          - release
        default: debug
      iconB64:
        description: 自定义启动图标（PNG 的 base64，≤36KB；留空使用仓库默认图标）
        required: false
        default: ''

permissions:
  contents: read
  actions: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 安装 JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: 安装 Android SDK / NDK
        # kernel-core 的 C 桥（libbridge）需要 NDK 交叉编译；libclash.so 为预编译产物，无需 Go 工具链
        # 注意：ubuntu runner 的 PATH 不含 sdkmanager，需用 $ANDROID_SDK_ROOT 完整路径调用
        run: |
          set -e
          SDKMANAGER="$(command -v sdkmanager || echo "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager")"
          test -x "$SDKMANAGER" || { echo "::error::找不到 sdkmanager（ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT）"; exit 1; }
          yes | "$SDKMANAGER" --licenses >/dev/null 2>&1 || true
          "$SDKMANAGER" "platforms;android-36" "build-tools;36.0.0" "ndk;28.2.13676358"

      - name: 还原发布签名
        if: inputs.buildType == 'release'
        env:
          STORE_B64: ${{ secrets.SLTE_RELEASE_STORE_B64 }}
        run: |
          test -n "$STORE_B64" || { echo "::error::缺少 Secret SLTE_RELEASE_STORE_B64（release 构建必需，见 AppForge 签名面板）"; exit 1; }
          echo "$STORE_B64" | base64 -d > release.keystore
          # 自动探测密钥别名：兼容标准 keystool 密钥库与无 friendlyName 的第三方密钥库
          ALIAS="$(keytool -list -keystore release.keystore -storepass "${{ secrets.SLTE_RELEASE_STORE_PASSWORD }}" 2>/dev/null | awk -F',' '/PrivateKeyEntry/{print $1}' | head -1 | tr -d ' ')"
          if [ -n "$ALIAS" ]; then
            echo "SLTE_DETECTED_KEY_ALIAS=$ALIAS" >> "$GITHUB_ENV"
          fi

      - name: 应用自定义图标
        # 图标经构建输入（iconB64）传入，不写入仓库；兼容仓库内 app/icon-source.png（旧机制）。
        # 用 ImageMagick 生成五个密度 × 方形/圆形启动图标，覆盖 checkout 出来的
        # res/mipmap-*/ic_launcher*.png；同名覆盖即可换图标，不改源码与 Manifest。
        if: inputs.iconB64 != '' || hashFiles('app/icon-source.png') != ''
        env:
          ICON_B64: ${{ inputs.iconB64 }}
        run: |
          set -e
          if [ -n "$ICON_B64" ]; then
            echo "$ICON_B64" | base64 -d > app/icon-source.png
          fi
          test -f app/icon-source.png || { echo "无图标源，跳过"; exit 0; }
          if ! command -v convert >/dev/null 2>&1; then
            sudo apt-get update -qq && sudo apt-get install -y -qq imagemagick
          fi
          for entry in "mdpi 48" "hdpi 72" "xhdpi 96" "xxhdpi 144" "xxxhdpi 192"; do
            set -- $entry
            density=$1
            size=$2
            dir="app/src/main/res/mipmap-$density"
            # 居中裁成正方形并缩放到目标密度
            convert app/icon-source.png -resize "${size}x${size}^" -gravity center \
              -extent "${size}x${size}" "$dir/ic_launcher.png"
            # 圆形遮罩生成 round 图标（launcher 圆形场景）
            convert -size "${size}x${size}" xc:none -fill white \
              -draw "circle $((size/2)),$((size/2)) $((size/2)),1" "$dir/_mask.png"
            convert "$dir/ic_launcher.png" "$dir/_mask.png" -alpha off \
              -compose CopyOpacity -composite "$dir/ic_launcher_round.png"
            rm -f "$dir/_mask.png"
          done

      - name: 单元测试 + 编译
        env:
          SLTE_APP_NAME: ${{ inputs.appName }}
          SLTE_APPLICATION_ID: ${{ inputs.applicationId }}
          SLTE_API_BASE_URL: ${{ inputs.apiBaseUrl }}
          SLTE_API_TYPE: ${{ inputs.apiType }}
          SLTE_REMOTE_CONFIG_URLS: ${{ inputs.remoteConfigUrls }}
          SLTE_ALLOWED_DOMAINS: ${{ inputs.allowedDomains }}
          SLTE_NOTIFICATION_TITLE: ${{ inputs.notificationTitle }}
          SLTE_NOTIFICATION_TRAFFIC: ${{ inputs.notificationTraffic }}
          SLTE_CRISP_WEBSITE_ID: ${{ inputs.crispWebsiteId }}
          SLTE_CRISP_ENABLED: ${{ inputs.crispEnabled }}
          # 海外 runner 直连官方仓库（aliyun 镜像对海外不稳定会导致依赖解析失败）
          SLTE_USE_MIRROR: 'false'
          # release 签名（debug 构建不设，构建脚本自动跳过）
          SLTE_RELEASE_STORE_FILE: ${{ inputs.buildType == 'release' && 'release.keystore' || '' }}
          SLTE_RELEASE_STORE_PASSWORD: ${{ secrets.SLTE_RELEASE_STORE_PASSWORD }}
          # 别名优先用还原签名步骤自动探测的结果，兼容第三方密钥库
          SLTE_RELEASE_KEY_ALIAS: ${{ env.SLTE_DETECTED_KEY_ALIAS || secrets.SLTE_RELEASE_KEY_ALIAS }}
          SLTE_RELEASE_KEY_PASSWORD: ${{ secrets.SLTE_RELEASE_KEY_PASSWORD }}
        run: |
          chmod +x gradlew
          # versionCode 留空时按时间戳自动生成（单调递增，可覆盖旧包）
          VC="${{ inputs.versionCode }}"
          if [ -z "$VC" ]; then VC=$(( $(date +%s) / 60 )); fi
          export SLTE_VERSION_CODE="$VC"
          export SLTE_VERSION_NAME="${{ inputs.versionName }}"
          ./gradlew :app:testDebugUnitTest :app:assemble${{ inputs.buildType }}

      - name: 上传产物
        uses: actions/upload-artifact@v4
        with:
          name: SLTE-${{ inputs.versionName }}
          path: app/build/outputs/apk/*/SLTE-*.apk
          if-no-files-found: error
          retention-days: 90
